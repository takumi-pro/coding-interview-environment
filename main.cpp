#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define repi(i, init, n) for (int i = init; i < (int)(n); i++)

int main(void) {
  int test[] = {1,2,3,4,5};
  for (auto t : test) {
    cout << t << endl;
  }
  return 0;
}